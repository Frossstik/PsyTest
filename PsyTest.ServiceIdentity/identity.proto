service ServiceIdentity {
    rpc HasActiveSubscription (UserIdRequest) returns (SubscriptionResponse);
}

message UserIdRequest {
    string userId = 1;
}

message SubscriptionResponse {
    bool active = 1;
}
